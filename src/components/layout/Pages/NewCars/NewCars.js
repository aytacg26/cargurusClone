import React from 'react';
import PageContainer from '../PageContainer/PageContainer';

const NewCars = () => {
  return <PageContainer>This is the page for New Cars...</PageContainer>;
};

export default NewCars;
